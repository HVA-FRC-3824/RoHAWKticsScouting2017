<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="frc3824.rohawkticsscouting2017">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="18" />

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="frc3824.rohawkticsscouting2017.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity android:name=".Activities.Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.CloudStorage" />
        <activity android:name=".Activities.EventView" />
        <activity android:name=".Activities.MatchList" />
        <activity android:name=".Activities.MatchPlanning" />
        <activity android:name=".Activities.MatchScouting" />
        <activity android:name=".Activities.MatchView" />
        <activity android:name=".Activities.PitScouting" />
        <activity android:name=".Activities.RankingsView" />
        <activity android:name=".Activities.Schedule" />
        <activity android:name=".Activities.ScheduleBuilder" />
        <activity android:name=".Activities.Server" />
        <activity android:name=".Activities.Settings" />
        <activity android:name=".Activities.SuperScouting" />
        <activity android:name=".Activities.TeamList" />
        <activity android:name=".Activities.TeamView" />
    </application>
</manifest>